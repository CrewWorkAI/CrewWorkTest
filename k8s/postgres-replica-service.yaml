apiVersion: v1
kind: Service
metadata:
  name: haiku-postgres-replica
  namespace: haiku-battle
  labels:
    app.kubernetes.io/component: postgres-replica
spec:
  selector:
    app.kubernetes.io/component: postgres-replica
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
