apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: haiku-backend-metrics
  namespace: haiku-battle
  labels:
    k8s-app: haiku-backend
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: backend
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
